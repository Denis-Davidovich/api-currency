{% extends 'base.html.twig' %}

{% block title %}{{ app_name }}{% endblock %}

{% block body %}
    <h1>{{ app_name }}</h1>
    <p>PHP: {{ php_version }}</p>
    <p>Symfony: {{ symfony_version }}</p>
{% endblock %}
